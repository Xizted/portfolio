<nav
  id='navbar'
  class='fixed top-0 left-0 right-0 bg-gray-900/95 backdrop-blur-sm z-50 transition-transform duration-300 -translate-y-full'
>
  <div class='container mx-auto px-6'>
    <div class='flex items-center justify-between h-16'>
      <a href='#home' class='text-white font-bold text-xl'> Osmar Ortiz </a>
      <div class='hidden md:flex space-x-8'>
        <a
          href='#about-me'
          class='text-gray-300 hover:text-white transition-colors'>Sobre Mí</a
        >
        <a
          href='#technologies'
          class='text-gray-300 hover:text-white transition-colors'
          >Tecnologías</a
        >
        <a
          href='#timeline'
          class='text-gray-300 hover:text-white transition-colors'
          >Experiencia</a
        >
        <a
          href='#projects'
          class='text-gray-300 hover:text-white transition-colors'>Proyectos</a
        >
        <a
          href='#contact'
          class='text-gray-300 hover:text-white transition-colors'>Contacto</a
        >
      </div>
    </div>
  </div>
</nav>

<script>
  const navbar = document.getElementById('navbar');

  const handleScroll = () => {
    const homeSection = document.getElementById('home');
    if (!homeSection || !navbar) return;

    const homeBottom = homeSection.offsetTop + homeSection.offsetHeight;
    if (window.scrollY > homeBottom - 100) {
      navbar.classList.remove('-translate-y-full');
      navbar.classList.add('translate-y-0');
    } else {
      navbar.classList.remove('translate-y-0');
      navbar.classList.add('-translate-y-full');
    }
  };

  window.addEventListener('scroll', handleScroll);
</script>
